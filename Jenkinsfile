pipeline {
    agent {
      label "docker"
    }
    stages {
        stage('build') {
            steps {
                echo "TODO: deploy to production environment"
            }
        }
    }
}