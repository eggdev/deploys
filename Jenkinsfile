pipeline {
    agent {
      label "docker"
    }
    stages {
        stage('build') {
            steps {
                echo "TODO: We are up and running!"
            }
        }
        stage('test') {
            steps {
               sh "npm test" 
            }
        }
    }
}